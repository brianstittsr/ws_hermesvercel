services:
  gateway:
    build:
      context: ./hermes-agent
      dockerfile: Dockerfile
    restart: unless-stopped
    user: "10000:10000"
    group_add:
      - "${DOCKER_GID:-998}"
    volumes:
      - hermes-data:/opt/data
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - HERMES_UID=10000
      - HERMES_GID=10000
      - GATEWAY_ALLOW_ALL_USERS=true
    command: ["hermes", "gateway", "run"]
    networks:
      - hermes-net

  dashboard:
    build:
      context: ./hermes-agent
      dockerfile: Dockerfile
    restart: unless-stopped
    user: "10000:10000"
    group_add:
      - "${DOCKER_GID:-998}"
    depends_on:
      - gateway
    ports:
      - "9120:9120"
    volumes:
      - hermes-data:/opt/data
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - HERMES_UID=10000
      - HERMES_GID=10000
      - GATEWAY_URL=http://gateway:8000
    command: ["hermes", "dashboard", "--host", "0.0.0.0", "--port", "9120", "--no-open", "--insecure"]
    networks:
      - hermes-net

volumes:
  hermes-data:

networks:
  hermes-net:
