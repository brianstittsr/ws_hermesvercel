{
  "name": "hermes-vercel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd hermes-agent/web && npm install",
    "build": "cd hermes-agent/web && npm run build && mkdir -p ../../dist && cp -r ../hermes_cli/web_dist/* ../../dist/"
  }
}
